model:
  name: 'hurricane'
  freeze_encoder: true
  output_size: 5

train:
  batch_size: 1
  n_epochs: 1
  num_workers: 4

optimizer:
  type: 'adamw'
  lr: 0.0001
  weight_decay: 0.001

loss:
  dice_weight: 1.0
  focal_weight: 6.0
